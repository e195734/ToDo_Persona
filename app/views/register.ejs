<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>ToDoリスト登録</title>
    <link rel="stylesheet" href="/css/register.css"/>
  </head>
  <body>
    <div class="title"><h1>アカウント新規登録</h1></div>
    <div class="inner">
      <form action="/register_account" method="post">
        <p>
        ユーザーID<br>
        <input type="text" id="user-id" name="register_userid" required="required" maxlength="20" pattern="^[0-9A-Za-z]+$" placeholder="半角英数字のみ，20字まで．(例:IamHoge123)">
        </p>
        <p>
        ユーザーネーム<br>
        <input type="text" id="user-name" name="register_username" required="required" maxlength="20" placeholder="20字まで．(例:hogetarou)">
        </p>
        <p>
        メールアドレス<br>
        <input type="email" id="user-id" name="register_mail" required="required" placeholder="hoge@hoge.com">
        </p>
        <p>
        パスワード<br>
        <input type="password" id="password" name="register_password" required="required" maxlength="20" pattern="^[0-9A-Za-z]+$" placeholder="半角英数字のみ．20字まで．(例:hoge1234)">
        </p>
        <% if(typeof r_log !== 'undefined' ){%>
          <p><%= r_log %></p>
        <% } %>
        <button class="register-button" type="submit"> 登録</button>
      </form>
    </div>
    <div class="link">
      <a onclick="location.href='login'">ログインページへ</a>&nbsp;
      <a onclick="location.href='/'">メインページへ</a>
    </div>
  </body>
</html>
